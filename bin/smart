#!/usr/bin/env node

var path  = require('path'),
fs        = require('fs'),
lib       = path.join(path.dirname(fs.realpathSync(__filename)), '../lib'),
arguments = process.argv;

var smart = require(lib + '/smart-cli.js');
 
var a1 = arguments[2];

if (a1 === 'new') {
	smart.new(arguments[3]);
}
else {

}
